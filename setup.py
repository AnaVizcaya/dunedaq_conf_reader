from setuptools import setup

# Metadata goes in setup.cfg. These are here for GitHub's dependency graph.
setup(
    name="offline_conf_reader",
    install_requires=[
    ],
    extras_require={"develop": [
    ]},
)
